---
description: Vue 3 + Tailwind CSS v4 前端编码风格
globs: src/**/*.vue,src/**/*.ts,src/**/*.css
alwaysApply: false
---

# 前端编码风格（Vue 3 + Tailwind v4）

## Vue 组件
- 一律使用 `<script setup lang="ts">`，块顺序：script → template → style（尽量不写 style 块）
- 组件文件名与组件名用多单词 PascalCase（如 `SearchBar.vue`）；可复用逻辑抽成 `useXxx` 组合式函数，放在 `src/composables/`
- TS 已开启 strict：禁止 `any`，不滥用非空断言 `!`
- 导入统一用 `@/` 别名，不写 `../../` 相对路径
- props / emits 使用类型式声明，不用运行时对象声明：

```vue
<script setup lang="ts">
const props = defineProps<{ query: string; limit?: number }>();
const emit = defineEmits<{ select: [id: string] }>();
</script>
```

- 跨组件状态优先用组合式函数共享（模块级 `ref` + `useXxx`），确有必要再引入 Pinia

## 组件拆分与文件组织
- 目录约定：
  - `src/components/` —— UI 组件，按功能建子目录（如 `components/launcher/SearchBar.vue`）
  - `src/composables/` —— 有状态的业务逻辑（`useXxx.ts`），一个文件只导出一个 composable
  - `src/lib/` —— 与 Vue 无关的纯函数、Tauri invoke/listen 封装
- 一个 SFC 只做一件事；`App.vue` 只做布局与顶层编排，不堆业务
- 出现以下信号必须拆分，不要把逻辑都堆在一个文件里：
  - 组件超过约 150 行，或 template 嵌套超过 3~4 层 → 抽子组件
  - `v-for` 循环体超过几行 → 抽成 `XxxItem.vue`
  - template 中重复出现的结构 → 抽公共子组件
  - script 里出现与渲染无关的业务逻辑（防抖、数据变换、后端调用编排）→ 移到 composable 或 `src/lib/`，组件只负责绑定
- 数据流向：父传子用 props，子通知父用 emit，不直接改父级状态；仅在层级很深时用 provide/inject

## Tailwind CSS v4
- 本项目用 v4 的 CSS-first 配置：主题定制写在 `src/index.css` 的 `@theme` 块里，**不要创建 `tailwind.config.js`**
- 样式一律写工具类，不写自定义 class、scoped style 或 `@apply`，除非工具类无法表达（复杂动画、第三方内容选择器）
- 类名按 布局 → 尺寸/间距 → 排版 → 颜色/背景 → 边框/效果 → 状态变体 的顺序书写，保持一致
- 动态样式在完整类名之间做条件切换，禁止拼接类名片段：

```vue
<!-- ✅ 完整类名，Tailwind 能静态扫描到 -->
<div :class="active ? 'bg-blue-500' : 'bg-gray-200'" />
<!-- ❌ 拼接片段不会被生成 -->
<div :class="`bg-${color}-500`" />
```

## 图标
- 用 unplugin-icons + lucide：`import IconSearch from "~icons/lucide/search"`，作为组件 `<IconSearch class="size-4" />` 使用，不引入其他图标库

## 与 Tauri 交互
- `invoke` / `listen` 封装在 `src/lib/` 下的模块中并给出类型签名，组件里不直接散落字符串命令名
- 事件名与后端保持 kebab-case（如 `launcher-open`）
